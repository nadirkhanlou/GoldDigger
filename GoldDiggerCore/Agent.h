#pragma once

namespace GoldDiggerAgent {
	
	class Agent {
	private:
		unsigned int _n, _m;			// Dimensions of the map the agent is in
		unsigned int _currentPos;		// Current position of the agent on the map

	};
} // namespace goldDiggerAgent